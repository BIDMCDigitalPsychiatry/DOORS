import * as React from 'react';
import { Typography, Grid } from '@material-ui/core';

export default function Help() {
  return (
    <Grid container alignItems='center'>
      <Grid item>
        <Typography variant='h5'>Help Placeholder</Typography>
      </Grid>
    </Grid>
  );
}
