import * as React from 'react';
import { Typography, Grid } from '@material-ui/core';

export default function Profile() {
  return (
    <Grid container alignItems='center'>
      <Grid item>
        <Typography variant='h5'>Profile Placeholder</Typography>
      </Grid>
    </Grid>
  );
}
