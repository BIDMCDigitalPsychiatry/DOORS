import * as React from 'react';
import { Typography, Grid } from '@material-ui/core';

export default function Sessions() {
  return (
    <Grid container alignItems='center'>
      <Grid item>
        <Typography variant='h5'>Sessions Placeholder</Typography>
      </Grid>
    </Grid>
  );
}
